// The OpenUI Lang system prompt is generated from the installed component
// library (see scripts/generate-openui-prompt.mjs) and committed as a static
// string so the server/Mastra graph never has to load React component modules.
export { generatedOpenUISystemPrompt as portfolioOpenUISystemPrompt } from "./generated-system-prompt";
